{
  "name": "@prune/cost-security",
  "version": "0.1.0",
  "description": "Cost-Security — defend the token bill against adversarial/runaway spend. Deterministic, fail-open guards: tool-result token/expansion-bomb quarantine + output bounding, file-state thrash detection, and per-source injection-cost attribution.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/diff-enforcer": "0.1.0",
    "@prune/response-tuner": "0.1.0",
    "@prune/shared": "0.1.0",
    "@prune/tokenizer": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
