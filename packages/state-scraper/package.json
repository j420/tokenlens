{
  "name": "@prune/state-scraper",
  "version": "0.1.0",
  "description": "Read Cursor's local state for zero-key usage tracking (uses sqlite3 CLI)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0",
    "vitest": "^1.2.0"
  }
}
