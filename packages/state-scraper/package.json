{
  "name": "@prune/state-scraper",
  "version": "0.1.0",
  "description": "Read Cursor's local state for zero-key usage tracking (uses sql.js WASM)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/shared": "0.1.0",
    "sql.js": "^1.10.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/sql.js": "^1.4.9",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
