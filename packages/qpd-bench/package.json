{
  "name": "@prune/qpd-bench",
  "version": "0.1.0",
  "description": "F4 — Pareto Quality-per-Dollar Bench. Measures, on the user's own tasks, which model tiers are statistically quality-equivalent to the current one at lower cost. Recommends only; the user always picks the model.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/quality": "0.1.0",
    "@prune/equivalence": "0.1.0",
    "@prune/shared": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
