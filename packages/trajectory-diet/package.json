{
  "name": "@prune/trajectory-diet",
  "version": "0.1.0",
  "description": "F1 — Trajectory Diet. Predicts which agent retrieval steps had low influence on the final output and advises skipping similar steps next time. Advisory-only; never auto-skips, never alters output.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/telemetry": "0.1.0",
    "@prune/equivalence": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
