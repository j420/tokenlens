{
  "name": "@prune/tokenizer",
  "version": "0.1.0",
  "description": "Local token counting for OpenAI and Anthropic models",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/shared": "0.1.0",
    "gpt-tokenizer": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
