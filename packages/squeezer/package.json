{
  "name": "@prune/squeezer",
  "version": "0.1.0",
  "description": "TypeScript Compiler API-based code compression for token optimization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "typescript": "^5.3.3",
    "@prune/shared": "0.1.0",
    "@prune/tokenizer": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "vitest": "^1.2.0"
  }
}
