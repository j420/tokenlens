{
  "name": "@prune/tab-auditor",
  "version": "0.1.0",
  "description": "IDE Open-Tab Auditor (Phase-8 Tier-1). Scores every open editor tab for relevance to the current task using structural signals (path/import-graph adjacency, recency, task token overlap, size penalty) and recommends which tabs to drop from auto-attached AI context, with honest null-aware token savings.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prune/repo-map": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
