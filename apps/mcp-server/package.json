{
  "name": "@prune/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for AI self-regulation with Prune tools",
  "main": "dist/index.js",
  "bin": {
    "prune-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@prune/tokenizer": "workspace:*",
    "@prune/squeezer": "workspace:*",
    "@prune/state-scraper": "workspace:*",
    "@prune/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  }
}
